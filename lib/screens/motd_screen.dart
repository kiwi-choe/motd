import 'package:flutter/material.dart';

class MotdScreen extends StatelessWidget {
  const MotdScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("MOTD 포스터 및 설명할까요?"),
    );
  }
}
